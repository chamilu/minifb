import React, { Component } from 'react';

const styles = {
  outer: {
    textAlign: 'center',
    paddingTop: 30,
  },
};

export default class App extends Component {
  render() {
    return (
      <div style={styles.outer}>
        <p>Chamil Udayanga</p>
      </div>
    );
  }
}
